import { withRouter } from 'next/router'
import BlogPost from '../../components/layouts/blog-post'

export const meta = {
  published: true,
  publishedAt: '2020-02-22',
  title: 'recursive newton euler algorithm',
  // title: '[algorithm] recursive newton euler',
  summary: '',
  image: '',
}

export default withRouter(({ children, router }) => (
  <BlogPost path={router.pathname} meta={meta}>
    {children}
  </BlogPost>
))

## overview

The recursive newton euler algorithm is used to solve inverse dynamics of a
rigid body system that can be described by a kinematic tree. This algorithm was
first described in [[1]](#references-1), then later reworked to be simpler, more
efficient by utilizing a generalized coorderinate representation coupled with
spatial vector in [[2]](#references-2) 

<!--
What problem the algorithm is solving.
What the formulation of that problem is.
What is the input to the algorithm.
What is the output of the algorithm.
What is the space/time complexity of the algorithm.
-->

## background

We can represent any rigid body system in any configuration as the tuple $(\textit{model},\textit{state})$

Given a rigid body system comprised of $N_B$ bodies:

Let $\lambda(1)...\lambda(N_B)$ be the 


A rigid body system without closed-loops can be represented as a kinematic tree. One
way of describing such a system is by the number of bodies $N_B$, a parent body array
$\lambda(1) ...\lambda(N_B)$,.

The state, $\textit{state}$, is a set of generalized positions $\bm{q}$,
velocities $\bm{\dot{q}}$, and accelerations $\bm{\ddot{q}}$.

Inverse dynamics is the problem of finding the forces of a rigid body system given its state.

$$
\bm{\tau} = \text{ID}(\textit{model},\textit{state})
$$

## formulation

The model of the rigid body a $\textit{model}$ 



The general formuation of the algorithm is as follows:

1. Compute the velocity and acceleration of each body in the tree.
2. Compute the forces required to produce these accelerations.
3. Compute the forces transmitted across the joints from the forces acting on
the bodies.

$$
\begin{aligned}
&v_0 = 0 \\
&a_0 = -a_g \\
&\text{for } i = 1 \text{ to } N_B \text{ do} \\
&\hspace{1.5em} []
\end{aligned}
$$

## references


<Anchor id="references-1">
  [1]  J. Luh, et al. "Resolved-Acceleration Control of Mechanical Manipulators," IEEE Trans. On Auto. Control, Vol. AC-25, No. 3, pp. 468– 474, June 1980.
</Anchor>
<Anchor id="references-2">
  [2]  R. Featherstone. Rigid Body Dynamics Algorithms. Springer, 2008.
</Anchor>

